<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/payment_receive_nav_graph"
    app:startDestination="@+id/navigation_payment_receive_fragment">

    <fragment
        android:id="@+id/navigation_payment_receive_fragment"
        android:name="chat.sphinx.payment_receive.ui.PaymentReceiveFragment"
        tools:layout="@layout/fragment_payment_receive">
        <argument
            android:name="argContactId"
            app:argType="long" />
        <argument
            android:name="argChatId"
            app:argType="long" />
    </fragment>

</navigation>
