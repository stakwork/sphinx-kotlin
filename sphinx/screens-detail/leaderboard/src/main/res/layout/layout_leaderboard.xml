<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/layout_leaderboard"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/headerBG"
    android:elevation="4dp">

    <include
        android:id="@+id/layout_leaderboard_user_photos"
        layout="@layout/layout_leaderboard_user_photos"
        android:layout_width="match_parent"
        android:layout_height="300dp"
        app:layout_constraintTop_toTopOf="parent"
        tools:layout_editor_absoluteX="0dp" />

    <include
        android:id="@+id/layout_leaderboard_list"
        layout="@layout/layout_leaderboard_list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/layout_leaderboard_user_photos" />


</androidx.constraintlayout.widget.ConstraintLayout>