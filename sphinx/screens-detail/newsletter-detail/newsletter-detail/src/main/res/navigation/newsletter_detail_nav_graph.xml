<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/newsletter_detail_nav_graph"
    app:startDestination="@+id/navigation_newsletter_detail_fragment">

    <fragment
        android:id="@+id/navigation_newsletter_detail_fragment"
        android:name="chat.sphinx.newsletter_detail.ui.NewsletterDetailFragment"
        tools:layout="@layout/fragment_newsletter_detail" >

        <argument
            android:name="argChatId"
            app:argType="long" />

        <argument
            android:name="argFeedUrl"
            app:argType="string" />

    </fragment>

</navigation>
