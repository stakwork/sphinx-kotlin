<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/chat_contact_nav_graph"
    app:startDestination="@+id/navigation_chat_contact_fragment">

    <fragment
        android:id="@+id/navigation_chat_contact_fragment"
        android:name="chat.sphinx.chat_contact.ui.ChatContactFragment"
        tools:layout="@layout/fragment_chat_contact" >
        <argument
            android:name="argContactId"
            app:argType="long" />
        <argument
            android:name="argChatId"
            app:argType="long" />
    </fragment>

</navigation>
